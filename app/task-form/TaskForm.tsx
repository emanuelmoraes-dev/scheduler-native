import {SCREEN_COLOR} from '@/constants/colors'
import {StyleSheet, View} from 'react-native'

export function TaskForm() {
  return (
    <View style={styles.taskForm}>
      
    </View>
  )
}

const styles = StyleSheet.create({
  taskForm: {
    flex: 1,
    backgroundColor: SCREEN_COLOR
  }
})
